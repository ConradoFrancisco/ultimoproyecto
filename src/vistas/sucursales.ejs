<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    
    <title>Document</title>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <a href="/" class="navbar-brand">Crud con node js y conexion a Mysql</a>
        
    </nav>
    <div class="col-7 mt-5 pr-1 ">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Sucursal</th>
                    <th>Locación</th>
                    <th>Nombre_Empleado</th>
                    <th>Apellido_Empleado</th>
                    <th>Edad</th>
                    <th>Contacto</th>
                </tr>
            </thead>
            <tbody>
                <% if(data.length != 0){ %>
                    <% for (let i = 0 ; i < data.length; i++){%>
                    <tr>
                        <td><%= data[i].SUCURSAL %></td>
                        <td><%= data[i].LOCACIÓN %></td>
                        <td><%= data[i].NOMBRE_EMPLEADO %></td>
                        <td><%= data[i].APELLIDO_EMPLEADO %></td>
                        <td><%= data[i].EDAD %></td>
                        <td><%= data[i].CONTACTO %></td>
                    </tr>
                <% }%>
                <% } else{%>
                    </tbody>
                    <div class="alert alert-warning" role="alert">
                        No hay empleados asignados a esta sucursal
                      </div>
                <%}%>
</body>
</html>